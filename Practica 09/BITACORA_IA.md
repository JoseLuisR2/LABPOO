En esta sesión se trabajó en el flujo de datos entre la memoria RAM y el disco duro. Se manejaron diferentes tipos de persistencia, desde archivos de texto plano hasta la serialización de objetos completos.

Ajustes realizados
Se corrigió la lógica de lectura del CSV para asegurar que cada columna del arreglo fuera asignada correctamente al constructor del objeto mediante índices, evitando errores de tipos incompatibles y asegurando la integridad de los datos numéricos.

En este caso se usó la IA a mayor nivel, no pude resolver un error que no me dejaba avanzar y tuve que investigar muchas mas cosas, la IA tambien me dio partes de código enteros para solucionar esta problematica pero ni asi fui capaz.